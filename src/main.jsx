import React from 'react';
import ReactDOM from 'react-dom/client';
import ConsensusSNS from './ConsensusSNS.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ConsensusSNS />
  </React.StrictMode>
);
